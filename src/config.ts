export default {
    credentials: {
        host: "localhost",
        user: "root",
        password: 'root',
        database: 'moodledistancia',
        dest: './data.sql' // destination file
    }, 
    exitOnError: false,
    showErrors: false
}