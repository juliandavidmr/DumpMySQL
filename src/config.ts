export default {
    credentials: {
        host: "localhost",
        user: "root",
        password: 'root',
        database: 'moodledistancia',
        dest: 'out.sql' // destination file
    }, 
    exitOnError: false,
    showErrors: false
}