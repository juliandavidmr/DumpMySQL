export default {
    credentials: {
        host: "localhost",
        user: "root",
        password: 'root',
        database: 'prueba',
        dest: './data.sql' // destination file
    }, 
    exitOnError: false
}